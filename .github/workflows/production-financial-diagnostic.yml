name: InCheck360 Production Financial Diagnostic

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type RUN to execute the production financial diagnostic'
        required: true
        default: 'RUN'
  push:
    branches:
      - main
    paths:
      - 'scripts/production-financial-diagnostic-runner.js'
      - 'scripts/production-financial-e2e-runner.js'
      - 'notification-template-helpers.js'
      - '.github/workflows/production-financial-diagnostic.yml'

permissions:
  contents: read

jobs:
  production-financial-diagnostic:
    name: Production financial diagnostic
    if: github.event_name == 'push' || github.event.inputs.confirm == 'RUN'
    runs-on: ubuntu-latest
    timeout-minutes: 25

    env:
      E2E_WRITE_CONFIRM: RUN
      TEST_APP_URL: ${{ secrets.TEST_APP_URL }}
      TEST_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
      TEST_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY }}
      TEST_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SUPABASE_SERVICE_ROLE_KEY }}
      TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run production financial diagnostic
        run: node scripts/production-financial-diagnostic-runner.js

      - name: Upload financial diagnostic report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: incheck360-production-financial-diagnostic-${{ github.run_number }}
          path: test-results/production-write-e2e.json
          if-no-files-found: warn
          retention-days: 14
