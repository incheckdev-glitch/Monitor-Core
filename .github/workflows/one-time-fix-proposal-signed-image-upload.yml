name: One-time Fix Proposal Signed Image Upload

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-fix-proposal-signed-image-upload.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Patch signed proposal image upload
        run: python3 .github/scripts/patch-proposal-signed-image-upload.py
      - name: Focused validation
        run: |
          node --check proposals.js
          node tests/proposal-signed-image-upload.test.js
      - name: Full validation
        run: npm run check
      - name: Commit fix
        run: |
          git config user.name "incheckdev-glitch"
          git config user.email "incheckdev@gmail.com"
          git add proposals.js index.html tests/proposal-signed-image-upload.test.js
          git diff --cached --quiet && exit 0
          git commit -m "fix: accept signed proposal images as PDF"
          git push origin HEAD:main

# rerun after cache-key regression compatibility update
