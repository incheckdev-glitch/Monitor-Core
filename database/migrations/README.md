# Future database migrations

This directory intentionally starts empty in the clean master. Add only new, generic migrations here using `YYYYMMDD_description.sql`. Historical client-specific patches are intentionally excluded.
